{
  "name": "single-use-file",
  "version": "1.0.1",
  "description": "A simple library which writes a file, and then deletes it when read.",
  "main": "index.js",
  "scripts": {
    "pretest": "./node_modules/.bin/standard --verbose | ./node_modules/.bin/snazzy",
    "test": "./node_modules/.bin/tape ./test/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxnachlinger/single-use-file.git"
  },
  "author": "Max Nachlinger",
  "license": "ISC",
  "devDependencies": {
    "snazzy": "^7.1.1",
    "standard": "^11.0.1",
    "tape": "^4.6.3",
    "tape-catch": "^1.0.6"
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {}
}
